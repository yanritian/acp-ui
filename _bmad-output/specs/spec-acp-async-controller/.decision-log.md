# Spec Decision Log

## 2026-06-07 — Initial Spec Creation

**Mode:** Create
**Source:** PRD + architecture refactor plan + conversation history + code review findings

**Verdict:** Pass 1 (Coherence) — All 8 capabilities have both intent and success signal. Constraints are actionable. At least one non-goal is explicit per category. Success signal is testable. Lean prose enforced.

Pass 2 (Preservation) — All load-bearing claims from the architecture plan landed in SPEC.md or companions. Wrapper-only content (BMad activation, skill loading) dropped and logged.

**open_questions logged:** 3 (Codex auth, mobile push relay, WeChat API)
**assumptions logged:** 3 (Claude Code installed, trusted local devices, user-managed tokens)

## Issues Found During Review

### Architecture-Level

1. **database.rs 结构混乱** — tool_calls 和 tool_calls_detail 两个表几乎相同，ALTER TABLE 错误被静默忽略
2. **Sync Engine checksum 太弱** — 字节求和容易被碰撞
3. **Bot 网关 5/6 适配器是桩** — 但 supports_* 返回 true，误导调用方

### Flutter UX

4. **审批 badge 硬编码为 '3'** — 应连接真实数据
5. **ApprovalCard 反馈文本泄露** — 切换卡片不清空
6. **ProgressScreen 重复过滤 4 次** — 性能问题
7. **发送按钮不响应输入** — 需要添加 controller listener
8. **失败任务显示 100% 进度** — 视觉误导

### Rust

9. **get_task_history 无分页** — 长期运行会 OOM
10. **SyncEngine clear() 使用 unwrap()** — 可能 panic
