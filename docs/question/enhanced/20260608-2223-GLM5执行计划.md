# ACP-UI GLM5 后续执行计划（可直接执行版）

> 本文档是给 GLM5 的执行指令，每个任务都包含精确的文件路径、行号、代码变更和验证步骤。
> 生成日期: 2026-06-08
> 基线 commit: `9d7bb84` (branch: `refactor/project-cleanup`)

---

## 执行总览

共 5 个阶段、18 个任务。每完成一个阶段执行 `git commit`。

```
阶段一（P0 紧急修复）：5 个任务，预计 1-2 小时
阶段二（P1 假数据清理）：4 个任务，预计 1 天
阶段三（P1 前后端打通）：4 个任务，预计 1-2 天
阶段四（P2 功能完善）：3 个任务，预计 1-2 天
阶段五（P3 架构优化）：2 个任务，预计 1 天
```

---

## 阶段一：P0 紧急修复

### 任务 1.1：修复 Flutter instruction_screen.dart 编译错误

**问题**：`_InstructionTextField` 的 `initState()` 和 `dispose()` 方法被重复定义了两次（第 318-329 行和第 332-343 行完全相同），Dart 编译器会报错。

**文件**：`acp_ui_flutter/lib/screens/instruction_screen.dart`

**操作**：删除第 331-342 行（第二个重复的 `initState()` 和 `dispose()` 方法）。

删除前（第 315-343 行）：
```dart
class _InstructionTextFieldState extends State<_InstructionTextField> {
  final TextEditingController _controller = TextEditingController();

  @override
  void initState() {
    super.initState();
    _controller.addListener(() => setState(() {}));
  }

  @override
  void dispose() {
    _controller.removeListener(() => setState(() {}));
    _controller.dispose();
    super.dispose();
  }

  @override
  void initState() {     // <-- 重复，删除这里
    super.initState();
    _controller.addListener(() => setState(() {}));
  }

  @override
  void dispose() {       // <-- 重复，删除这里
    _controller.removeListener(() => setState(() {}));
    _controller.dispose();
    super.dispose();
  }
```

**验证**：`cd acp_ui_flutter && flutter analyze` 无编译错误。

---

### 任务 1.2：修复 AgentConfigView.vue setInterval 内存泄漏

**问题**：`onMounted` 中创建的 2 秒轮询定时器未在 `onUnmounted` 中清除，页面反复进出会累积定时器导致内存泄漏和 CPU 浪费。

**文件**：`src/views/AgentConfigView.vue`

**操作**：

1. 在第 204 行的 import 中添加 `onUnmounted`：
```typescript
// 原: import { ref, computed, onMounted } from 'vue';
import { ref, computed, onMounted, onUnmounted } from 'vue';
```

2. 在组件顶层添加变量存储 interval ID（在 `onMounted` 之前）：
```typescript
let statusIntervalId: ReturnType<typeof setInterval> | null = null;
```

3. 修改第 420-423 行的 `onMounted`：
```typescript
onMounted(async () => {
  await loadConfig();
  await loadStatus();
  statusIntervalId = setInterval(loadStatus, 2000);
});
```

4. 添加 `onUnmounted` 清理：
```typescript
onUnmounted(() => {
  if (statusIntervalId !== null) {
    clearInterval(statusIntervalId);
    statusIntervalId = null;
  }
});
```

**验证**：`npx vue-tsc --noEmit` 通过。

---

### 任务 1.3：删除 bot_gateway.rs 孤儿代码

**问题**：`src-tauri/src/bot_gateway.rs`（1245 行）从未被 `lib.rs` 的 `mod` 声明引用，整个文件不会参与编译。它与已注册的 `bot_adapters.rs` 功能重叠，且包含 18 处 TODO、5 个 stub 适配器（钉钉/微信/企微/Telegram/Discord）、7 个与实现矛盾的测试断言。

**文件**：`src-tauri/src/bot_gateway.rs`

**操作**：直接删除整个文件。

```bash
# 移到回收站而非永久删除（Windows）
powershell.exe -Command "Add-Type -AssemblyName Microsoft.VisualBasic; [Microsoft.VisualBasic.FileIO.FileSystem]::DeleteFile('D:\dingsun\acp-ui\src-tauri\src\bot_gateway.rs', 'OnlyErrorDialogs', 'SendToRecycleBin')"
```

或者更简单：`git rm src-tauri/src/bot_gateway.rs`（因为文件已在 git 中跟踪）。

**验证**：`cargo check`（如有 Rust 工具链）无变化，因为该文件从未被编译。

---

### 任务 1.4：处理 hermes_traits.rs 死代码

**问题**：`src-tauri/src/hermes_traits.rs`（633 行）被编译但其类型和函数在项目其他任何位置都未被使用。所有核心方法实现都是模拟/硬编码的（如 `call_mcp_tool` 直接返回 params，`invoke_skill` 硬编码返回 `duration_ms: 500`）。

**文件**：`src-tauri/src/lib.rs` 第 41 行

**操作**：注释掉 `mod hermes_traits;` 声明：

```rust
// 原（第 41 行）:
mod hermes_traits;    // Hermes Traits - Self-Evolving, Self-Healing, MCP-Integrated, Skill-Capable (Phase 4)

// 改为:
// mod hermes_traits;    // TODO: Re-enable when integrating with hermes_flow and MCP client
```

同时注释掉文件中对 `hermes_traits` 类型的任何引用（如果有的话）。

**验证**：`cargo check` 无新错误。

---

### 任务 1.5：UnifiedSettingsView 设置持久化

**问题**：Advanced 标签页的 `tokenBudget`、`logLevel` 和 General 的 `theme` 仅为组件内 `ref`，不与任何 store 或后端同步。用户修改后刷新页面设置丢失。

**文件**：`src/shared/ui/UnifiedSettingsView.vue` 第 96-98 行

**操作**：

1. 使用 `localStorage` 持久化这三个设置值：

```typescript
// 替换第 96-98 行:
// const theme = ref('auto');
// const tokenBudget = ref(100000);
// const logLevel = ref('info');

const theme = ref(localStorage.getItem('acp-ui:theme') ?? 'auto');
const tokenBudget = ref(Number(localStorage.getItem('acp-ui:tokenBudget')) || 100000);
const logLevel = ref(localStorage.getItem('acp-ui:logLevel') ?? 'info');

// 添加 watch 持久化
import { watch } from 'vue';
watch(theme, (v) => localStorage.setItem('acp-ui:theme', v));
watch(tokenBudget, (v) => localStorage.setItem('acp-ui:tokenBudget', String(v)));
watch(logLevel, (v) => localStorage.setItem('acp-ui:logLevel', v));
```

2. 在 `<script setup>` 中确保 `watch` 已从 vue 导入。

**验证**：`npx vue-tsc --noEmit` 通过。浏览器中修改设置 → 刷新页面 → 设置保留。

---

## 阶段二：P1 假数据清理（DeepSeek 核心质疑）

### 任务 2.1：TaskGraphView 假数据处理

**问题**：`router.ts` 第 27 行直接调用 `createMockTaskDag()` 作为 props 传入 TaskGraphView，展示的 DAG 图是写死的示例数据。

**文件**：
- `src/router.ts` 第 2 行和第 23-28 行
- `src/lib/feature-registry.ts` 第 39 行
- `src/lib/mock-task-dag.ts`（55 行，仅被 router.ts 引用）

**操作**：

**方案 A：标注为演示（短期推荐）**

1. 修改 `src/lib/feature-registry.ts` 第 39 行，添加 `experimental: true`：
```typescript
{ id: 'task-graph', labelKey: 'navigation.taskGraph', icon: '🔗', requiresAgent: false, descriptionKey: 'navigationDescriptions.taskGraph', group: 'workflow', experimental: true },
```

2. 修改 `src/router.ts` 第 27 行，在 props 中添加演示标记：
```typescript
props: () => ({ dag: createMockTaskDag(), showAgents: true, orientation: 'vertical', isDemo: true })
```
（注意：`isDemo: true` 已经存在，确保 TaskGraphView 在 `isDemo` 为 true 时显示一个明显的 "演示数据" 横幅。）

3. 修改 `src/features/workflow/TaskGraphView.vue`，在模板顶部添加演示横幅：
```html
<div v-if="isDemo" class="demo-banner">
  ⚠️ 当前展示的是示例数据。接入真实任务执行后将显示实际任务 DAG。
</div>
```
并添加对应 CSS（黄色背景横幅）。

**验证**：浏览器中打开 `/task-graph`，页面顶部出现黄色"演示数据"横幅。

---

### 任务 2.2：EnhancedHermesDashboard 假数据处理

**问题**：`EnhancedHermesDashboard.vue` 第 31 行有 `initializeMockData()` 函数，显式创建 5 个模拟 agent 和对应的 node/edge 数据。

**文件**：`src/features/hermes/EnhancedHermesDashboard.vue`

**操作**：

1. 在 `initializeMockData()` 函数内部，将整个函数体包裹在条件判断中，仅在没有真实数据时才加载 mock：

```typescript
function initializeMockData() {
  // 如果 store 已有真实数据，跳过 mock 初始化
  if (collaborationStore.nodes.length > 0) return;

  // ... 原有 mock 代码保持不变 ...
}
```

2. 在模板顶部添加演示状态横幅（当使用 mock 数据时显示）：

```html
<div v-if="isUsingMockData" class="demo-banner">
  ⚠️ 当前展示的是模拟协作网络数据。当 Agent 团队实际运行时将显示真实协作关系。
</div>
```

3. 添加计算属性：
```typescript
const isUsingMockData = computed(() => {
  // 如果没有运行中的团队协作任务，说明是 mock 数据
  return !collaborationStore.isRunning;
});
```

**验证**：`npx vue-tsc --noEmit` 通过。

---

### 任务 2.3：HermesDashboard 数据源确认

**问题**：文档 01-3.1 标注 HermesDashboard 为"全部模拟数据"。

**当前状态（审计结果）**：`HermesDashboard.vue` 已改为使用 `useHermesApi()`（`src/lib/hermes-api.ts`），该 API 通过 Tauri event listener 获取实时数据。**不是纯假数据了**。但当 Hermes 后端不可用时，所有值初始化为 0/空。

**操作**：

1. 在 `HermesDashboard.vue` 模板中添加连接状态指示：

```html
<div v-if="!connected" class="connection-banner">
  Hermes 后端未连接。当前显示的为初始值，连接后将展示实时数据。
</div>
```

其中 `connected` 来自第 39 行的 `const connected = computed(() => hermesApi.connected.value);`。

**验证**：`npx vue-tsc --noEmit` 通过。

---

### 任务 2.4：StatusView/MonitorView 已非空壳，补充空状态说明

**问题**：文档 01-3.3 标注 StatusView/MonitorView 为"空壳"。

**当前状态**：StatusView 使用 `useTeamRuntimeStore()` 展示任务统计，MonitorView 使用同 store 展示事件列表并嵌入 TrafficMonitor。**已非空壳**。但当没有运行中的团队任务时，两个页面只显示 "No tasks" / "No events"。

**操作**：

1. 在 `src/features/monitoring/StatusView.vue` 的空状态中添加引导说明：

```html
<!-- 替换第 33-35 行的 empty-state -->
<div v-else class="empty-state">
  <p>{{ t('common.noTasks') }}</p>
  <p class="hint">在"协作网络"或"Agent 团队"页面启动团队任务后，这里将显示实时状态。</p>
</div>
```

2. 在 `src/features/monitoring/MonitorView.vue` 的空状态中同样添加引导：

```html
<!-- 替换第 20-22 行的 empty-state -->
<div v-else class="empty-state">
  <p>{{ t('common.noEvents') }}</p>
  <p class="hint">Agent 运行时产生的事件将在这里展示。请先在对话页面启动 Agent。</p>
</div>
```

**验证**：`npx vue-tsc --noEmit` 通过。浏览器中两个页面显示引导文案。

---

## 阶段三：P1 前后端打通（23 个 Tauri 命令接入前端）

这是最重要的阶段。GLM5 创建了 `agent_orchestration.rs`（12 命令）、`approval_engine.rs`（4 命令）、`sync_engine.rs`（7 命令），但前端零调用。

### 任务 3.1：创建前端 API 封装层

**文件**：新建 `src/lib/orchestration-api.ts`

**操作**：创建封装文件，通过 `invokeOrProxy` 调用所有 23 个后端命令：

```typescript
// src/lib/orchestration-api.ts
// 前端 API 封装：对接 agent_orchestration / approval_engine / sync_engine 后端命令

import { invokeOrProxy } from './host';

// ============================================================
// Agent Orchestration (12 commands from agent_orchestration.rs)
// ============================================================

export interface OrchestratorAgent {
  id: string;
  name: string;
  capabilities: string[];
  cli_command?: string;
  cli_args?: string[];
  status: string;
}

export interface OrchestratorTask {
  id: string;
  agent_id: string;
  description: string;
  status: 'pending' | 'running' | 'completed' | 'failed' | 'cancelled';
  result?: string;
  created_at: string;
  completed_at?: string;
}

export interface WorkflowStage {
  stage_id: string;
  stage_name: string;
  agent_id?: string;
  status: string;
  input_data?: Record<string, unknown>;
  output_data?: Record<string, unknown>;
}

// Agent 注册表
export async function registerAgent(agent: Partial<OrchestratorAgent>): Promise<void> {
  return invokeOrProxy('orchestration_register_agent', { agent });
}

export async function listOrchestratorAgents(): Promise<OrchestratorAgent[]> {
  return invokeOrProxy('orchestration_list_agents');
}

export async function selectBestAgent(taskDescription: string): Promise<OrchestratorAgent> {
  return invokeOrProxy('orchestration_select_agent', { task_description: taskDescription });
}

// 任务执行
export async function executeTask(agentId: string, description: string): Promise<OrchestratorTask> {
  return invokeOrProxy('orchestration_execute_task', { agent_id: agentId, description });
}

export async function executeTaskAuto(description: string): Promise<OrchestratorTask> {
  return invokeOrProxy('orchestration_execute_task_auto', { description });
}

export async function getTaskStatus(taskId: string): Promise<OrchestratorTask> {
  return invokeOrProxy('orchestration_get_task_status', { task_id: taskId });
}

export async function cancelTask(taskId: string): Promise<void> {
  return invokeOrProxy('orchestration_cancel_task', { task_id: taskId });
}

export async function listActiveTasks(): Promise<OrchestratorTask[]> {
  return invokeOrProxy('orchestration_list_active_tasks');
}

export async function listTaskHistory(page?: number, pageSize?: number): Promise<OrchestratorTask[]> {
  return invokeOrProxy('orchestration_list_task_history', { page, page_size: pageSize });
}

// 工作流
export async function executeWorkflowStage(workflowId: string, stage: WorkflowStage): Promise<WorkflowStage> {
  return invokeOrProxy('orchestration_execute_workflow_stage', { workflow_id: workflowId, stage });
}

export async function executeFullWorkflow(workflowId: string, stages: WorkflowStage[]): Promise<OrchestratorTask[]> {
  return invokeOrProxy('orchestration_execute_full_workflow', { workflow_id: workflowId, stages });
}

// 种子数据
export async function seedDefaultAgents(): Promise<void> {
  return invokeOrProxy('orchestration_seed_default_agents');
}

// ============================================================
// Approval Engine (4 commands from approval_engine.rs)
// ============================================================

export interface ApprovalRequest {
  id: string;
  task_id: string;
  stage_name: string;
  request_type: string;
  description: string;
  status: 'pending' | 'approved' | 'rejected' | 'expired';
  created_at: string;
  decided_at?: string;
  decision?: string;
  feedback?: string;
}

export interface ApprovalStats {
  total: number;
  pending: number;
  approved: number;
  rejected: number;
  expired: number;
}

export async function getPendingApprovals(): Promise<ApprovalRequest[]> {
  return invokeOrProxy('approval_get_pending');
}

export async function getApprovalRequest(requestId: string): Promise<ApprovalRequest> {
  return invokeOrProxy('approval_get_request', { request_id: requestId });
}

export async function decideApproval(
  requestId: string,
  decision: 'approved' | 'rejected',
  feedback?: string,
): Promise<void> {
  return invokeOrProxy('approval_decide', { request_id: requestId, decision, feedback });
}

export async function getApprovalStats(): Promise<ApprovalStats> {
  return invokeOrProxy('approval_get_stats');
}

// ============================================================
// Sync Engine (7 commands from sync_engine.rs)
// ============================================================

export interface SyncEntity {
  entity_type: string;
  entity_id: string;
  data: Record<string, unknown>;
  version: number;
  source?: string;
  updated_at?: string;
}

export async function syncPutEntity(entity: SyncEntity): Promise<void> {
  return invokeOrProxy('sync_put_entity', { entity });
}

export async function syncGetEntity(entityType: string, entityId: string): Promise<SyncEntity | null> {
  return invokeOrProxy('sync_get_entity', { entity_type: entityType, entity_id: entityId });
}

export async function syncDeleteEntity(entityType: string, entityId: string): Promise<void> {
  return invokeOrProxy('sync_delete_entity', { entity_type: entityType, entity_id: entityId });
}

export async function syncListEntities(entityType: string): Promise<SyncEntity[]> {
  return invokeOrProxy('sync_list_entities', { entity_type: entityType });
}

export async function syncGetStats(): Promise<Record<string, number>> {
  return invokeOrProxy('sync_get_stats');
}

export async function syncReceiveEvent(event: Record<string, unknown>): Promise<void> {
  return invokeOrProxy('sync_receive_event', { event });
}

export async function syncFlushOffline(): Promise<void> {
  return invokeOrProxy('sync_flush_offline');
}
```

**验证**：`npx vue-tsc --noEmit` 通过。

---

### 任务 3.2：创建 Pinia Store 封装

**文件**：新建 `src/stores/orchestration.ts`

**操作**：创建 Pinia store 管理编排状态：

```typescript
// src/stores/orchestration.ts
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';
import {
  listOrchestratorAgents,
  listActiveTasks,
  listTaskHistory,
  getPendingApprovals,
  getApprovalStats,
  executeTaskAuto,
  cancelTask,
  decideApproval,
  seedDefaultAgents,
  type OrchestratorAgent,
  type OrchestratorTask,
  type ApprovalRequest,
  type ApprovalStats,
} from '@/lib/orchestration-api';

export const useOrchestrationStore = defineStore('orchestration', () => {
  // --- State ---
  const agents = ref<OrchestratorAgent[]>([]);
  const activeTasks = ref<OrchestratorTask[]>([]);
  const taskHistory = ref<OrchestratorTask[]>([]);
  const pendingApprovals = ref<ApprovalRequest[]>([]);
  const approvalStats = ref<ApprovalStats>({ total: 0, pending: 0, approved: 0, rejected: 0, expired: 0 });
  const isLoading = ref(false);
  const error = ref<string | null>(null);

  // --- Computed ---
  const runningTaskCount = computed(() =>
    activeTasks.value.filter(t => t.status === 'running').length
  );
  const hasPendingApprovals = computed(() => pendingApprovals.value.length > 0);

  // --- Actions ---
  async function refreshAgents() {
    try {
      agents.value = await listOrchestratorAgents();
    } catch (e) {
      console.warn('[Orchestration] Failed to load agents:', e);
    }
  }

  async function refreshActiveTasks() {
    try {
      activeTasks.value = await listActiveTasks();
    } catch (e) {
      console.warn('[Orchestration] Failed to load active tasks:', e);
    }
  }

  async function refreshTaskHistory() {
    try {
      taskHistory.value = await listTaskHistory();
    } catch (e) {
      console.warn('[Orchestration] Failed to load task history:', e);
    }
  }

  async function refreshApprovals() {
    try {
      pendingApprovals.value = await getPendingApprovals();
      approvalStats.value = await getApprovalStats();
    } catch (e) {
      console.warn('[Orchestration] Failed to load approvals:', e);
    }
  }

  async function refreshAll() {
    isLoading.value = true;
    error.value = null;
    try {
      await Promise.all([refreshAgents(), refreshActiveTasks(), refreshApprovals()]);
    } catch (e) {
      error.value = String(e);
    } finally {
      isLoading.value = false;
    }
  }

  async function submitTask(description: string) {
    try {
      const task = await executeTaskAuto(description);
      activeTasks.value.push(task);
      return task;
    } catch (e) {
      error.value = String(e);
      throw e;
    }
  }

  async function cancelActiveTask(taskId: string) {
    try {
      await cancelTask(taskId);
      const idx = activeTasks.value.findIndex(t => t.id === taskId);
      if (idx >= 0) activeTasks.value[idx].status = 'cancelled';
    } catch (e) {
      error.value = String(e);
    }
  }

  async function approveRequest(requestId: string, feedback?: string) {
    await decideApproval(requestId, 'approved', feedback);
    pendingApprovals.value = pendingApprovals.value.filter(r => r.id !== requestId);
    await refreshApprovals();
  }

  async function rejectRequest(requestId: string, feedback?: string) {
    await decideApproval(requestId, 'rejected', feedback);
    pendingApprovals.value = pendingApprovals.value.filter(r => r.id !== requestId);
    await refreshApprovals();
  }

  async function initDefaults() {
    try {
      await seedDefaultAgents();
      await refreshAgents();
    } catch (e) {
      console.warn('[Orchestration] Failed to seed defaults:', e);
    }
  }

  return {
    // state
    agents, activeTasks, taskHistory, pendingApprovals, approvalStats, isLoading, error,
    // computed
    runningTaskCount, hasPendingApprovals,
    // actions
    refreshAgents, refreshActiveTasks, refreshTaskHistory, refreshApprovals,
    refreshAll, submitTask, cancelActiveTask, approveRequest, rejectRequest, initDefaults,
  };
});
```

**验证**：`npx vue-tsc --noEmit` 通过。

---

### 任务 3.3：AgentTeamsDashboard 接入真实数据

**文件**：`src/views/AgentTeamsDashboard.vue`

**操作**：

1. 导入并使用新的 orchestration store，替换现有的模拟数据加载逻辑
2. 在 `onMounted` 中调用 `orchestrationStore.refreshAll()`
3. 将模板中的模拟 agent 列表替换为 `orchestrationStore.agents`
4. 将模拟任务列表替换为 `orchestrationStore.activeTasks`
5. 添加审批区域：当 `orchestrationStore.hasPendingApprovals` 时显示待审批列表

具体修改需阅读文件后实施，核心变更：
- 将硬编码的 agent 数组改为 `orchestrationStore.agents`
- 将硬编码的任务数组改为 `orchestrationStore.activeTasks`
- 添加"提交任务"表单，调用 `orchestrationStore.submitTask(description)`
- 添加"取消任务"按钮，调用 `orchestrationStore.cancelActiveTask(taskId)`

**验证**：`npx vue-tsc --noEmit` 通过。浏览器中打开 `/agent-teams`，页面加载时能看到来自后端的 agent 列表（Tauri 模式下）。

---

### 任务 3.4：DashboardView 接入编排数据

**文件**：`src/views/DashboardView.vue`

**操作**：

1. 在 DashboardView 中添加编排状态摘要卡片：
   - "活跃任务数" → `orchestrationStore.runningTaskCount`
   - "待审批" → `orchestrationStore.pendingApprovals.length`
   - "已注册 Agent 数" → `orchestrationStore.agents.length`

2. 在 `onMounted` 中调用 `orchestrationStore.refreshAll()`

3. 如果有待审批项，显示审批快捷入口

**验证**：`npx vue-tsc --noEmit` 通过。Dashboard 页面显示编排摘要数据。

---

## 阶段四：P2 功能完善

### 任务 4.1：权限弹窗改为非阻塞通知

**问题**：文档 01-2.3 指出 `PermissionDialog` 以模态弹窗阻塞整个 UI，用户无法在审批前查看 Traffic Monitor。

**文件**：
- `src/shared/dialogs/PermissionDialog.vue`（改为非阻塞横幅）
- `src/App.vue` 或主布局组件（放置横幅位置）

**操作**：

1. 创建新的非阻塞通知组件 `src/shared/ui/ApprovalBanner.vue`：
   - 顶部固定横幅（position: sticky, top: 0, z-index: 100）
   - 显示待审批数量徽章
   - 点击展开审批详情面板（非模态，不阻塞页面交互）
   - 审批/拒绝按钮
   - 完成后自动收起

2. 在主布局中（`App.vue` 或等效位置），在页面内容之前插入 `ApprovalBanner`

3. `ApprovalBanner` 使用 `useOrchestrationStore()` 获取 `pendingApprovals`

4. 保留原有的 `PermissionDialog.vue` 作为 fallback（Tauri 模式下仍可能触发模态权限请求）

**验证**：`npx vue-tsc --noEmit` 通过。

---

### 任务 4.2：会话管理增强 — 搜索/筛选/导出

**问题**：文档 02-四 指出会话列表无搜索/筛选/导出功能。

**文件**：
- `src/shared/layout/AppSidebar.vue`（添加搜索框和筛选器）
- `src/features/chat/SessionList.vue`（如果存在，添加导出按钮）

**操作**：

1. **会话搜索**：在 SessionList 顶部添加搜索输入框
   - 使用 `ref<string>('')` 存储搜索词
   - 通过 `computed` 过滤会话列表（按标题和 agent 名称模糊匹配）

2. **按 Agent 筛选**：添加下拉选择器
   - 从 `useConfigStore` 获取 agent 列表作为筛选选项
   - 选中后只显示该 agent 的会话

3. **会话导出**：在每个会话项上添加右键菜单或更多按钮
   - 导出为 Markdown：将消息列表格式化为 MD 格式
   - 导出为 JSON：直接序列化消息数组
   - 使用 `Blob` + `URL.createObjectURL` + `<a download>` 触发下载

4. **按时间分组**：将会话列表分为"今天"、"昨天"、"本周"、"更早"四个分组

**验证**：`npx vue-tsc --noEmit` 通过。侧边栏中搜索框可过滤会话，导出功能可生成文件。

---

### 任务 4.3：TokenOptimizerPanel 数据验证

**问题**：文档 01-3.4 指出 TokenOptimizerPanel 的 `getCacheStats()` 返回的数据链路未与真实 agent 调用打通。

**文件**：`src/features/plugins/TokenOptimizerPanel.vue`（943 行）

**操作**：

1. 审查 `TokenOptimizerPanel.vue` 中的数据来源：
   - 如果通过 `invokeOrProxy('plugin_get_stats')` 获取数据且后端已实现 → 无需修改
   - 如果数据来自 mock 或固定值 → 添加演示数据横幅

2. 在面板顶部添加连接状态指示：
```html
<div v-if="!hasRealData" class="demo-banner">
  ⚠️ 当前 Token 统计数据为模拟值。当 Agent 实际运行并启用 Token 优化插件后将显示真实数据。
</div>
```

3. 添加"压缩当前上下文"按钮（如果不存在）：
```typescript
async function compressNow() {
  try {
    await invokeOrProxy('plugin_compress_now', {});
    // 刷新统计
    await loadStats();
  } catch (e) {
    console.warn('[TokenOptimizer] Compress failed:', e);
  }
}
```

**验证**：`npx vue-tsc --noEmit` 通过。

---

## 阶段五：P3 架构优化

### 任务 5.1：Rust 模块按领域分组注释

**问题**：文档 03-七 指出 `lib.rs` 的 29+ 个模块平铺无分组。

**文件**：`src-tauri/src/lib.rs`

**操作**：在现有的 `mod` 声明前添加分组注释（仅添加注释，不移动文件以避免编译问题）：

```rust
// ============================================================================
// ---- 核心基础设施 ----
// ============================================================================
mod config;
mod database;
mod session_manager;
mod event_router;

// ============================================================================
// ---- ACP 传输层 ----
// ============================================================================
mod transport {
    pub mod websocket;
    pub mod tunnel;
}
// 注意：websocket 和 tunnel 目前作为独立 mod，未来可合并到 transport 子模块

// ============================================================================
// ---- Agent 管理 ----
// ============================================================================
mod agent;
mod agent_registry;
mod agent_config_parser;
// mod agent_bus;  // TODO: 如果存在则取消注释

// ============================================================================
// ---- 安全与权限 ----
// ============================================================================
mod permission_checker;
mod circuit_breaker;

// ============================================================================
// ---- 工作流与编排 ----
// ============================================================================
mod workflow_engine;
mod team_dag;
mod swarm_orchestrator;
mod agent_orchestration;
mod approval_engine;

// ============================================================================
// ---- 智能路由与自愈 ----
// ============================================================================
mod smart_router;
mod self_healing;

// ============================================================================
// ---- Hermes 系统 ----
// ============================================================================
mod hermes_flow;
// mod hermes_traits;  // TODO: Re-enable when integrated with hermes_flow

// ============================================================================
// ---- 数据同步 ----
// ============================================================================
mod sync_engine;

// ============================================================================
// ---- 插件系统 ----
// ============================================================================
mod plugin_registry;
mod hooks_executor;
mod skill_commands;
mod mcp_manager;
mod mcp_client;

// ============================================================================
// ---- Bot 适配器 ----
// ============================================================================
mod bot_adapters;
mod feishu_rich_message;

// ============================================================================
// ---- 执行代理 ----
// ============================================================================
mod executive_agent;

// ============================================================================
// ---- 可插拔功能模块 ----
// ============================================================================
mod finance;
mod gateway_config;

// ============================================================================
// ---- Tauri 命令处理 ----
// ============================================================================
mod commands;
```

**验证**：`cargo check` 无变化（仅修改了注释）。

---

### 任务 5.2：i18n 翻译键一致性检查脚本

**问题**：文档 01-5.1 指出 12 个语言文件翻译键数量不一致。

**文件**：新建 `scripts/check-i18n-consistency.ts`

**操作**：

```typescript
#!/usr/bin/env npx tsx
// scripts/check-i18n-consistency.ts
// 检查所有 locale 文件与 types.ts 的翻译键是否一致

import * as fs from 'fs';
import * as path from 'path';

const LOCALES_DIR = path.resolve(__dirname, '../src/locales');
const LOCALE_FILES = [
  'zh-CN.ts', 'en-US.ts', 'de-DE.ts', 'es-ES.ts', 'fr-FR.ts',
  'ja-JP.ts', 'ko-KR.ts', 'ms-MY.ts', 'ru-RU.ts', 'th-TH.ts', 'vi-VN.ts',
];

function extractKeys(content: string): Set<string> {
  const keys = new Set<string>();
  // 匹配 object property keys: keyName: 'value' or keyName: "value"
  const regex = /^\s+(\w+):\s*['"`{]/gm;
  let match;
  while ((match = regex.exec(content)) !== null) {
    keys.add(match[1]);
  }
  return keys;
}

function main() {
  const allKeys = new Map<string, Set<string>>();
  let hasErrors = false;

  for (const file of LOCALE_FILES) {
    const filePath = path.join(LOCALES_DIR, file);
    if (!fs.existsSync(filePath)) {
      console.error(`❌ Missing locale file: ${file}`);
      hasErrors = true;
      continue;
    }
    const content = fs.readFileSync(filePath, 'utf-8');
    allKeys.set(file, extractKeys(content));
  }

  // Use en-US as reference
  const refKeys = allKeys.get('en-US.ts');
  if (!refKeys) {
    console.error('❌ en-US.ts not found');
    process.exit(1);
  }

  console.log(`\nReference: en-US.ts has ${refKeys.size} keys\n`);

  for (const [file, keys] of allKeys) {
    if (file === 'en-US.ts') continue;
    const missing = [...refKeys].filter(k => !keys.has(k));
    const extra = [...keys].filter(k => !refKeys.has(k));

    if (missing.length > 0 || extra.length > 0) {
      hasErrors = true;
      console.log(`⚠️  ${file}: ${keys.size} keys`);
      if (missing.length > 0) console.log(`   Missing ${missing.length}: ${missing.slice(0, 10).join(', ')}${missing.length > 10 ? '...' : ''}`);
      if (extra.length > 0) console.log(`   Extra ${extra.length}: ${extra.slice(0, 5).join(', ')}`);
    } else {
      console.log(`✅ ${file}: ${keys.size} keys (matches reference)`);
    }
  }

  if (hasErrors) {
    console.log('\n❌ i18n keys are inconsistent. Please sync missing keys.');
    process.exit(1);
  } else {
    console.log('\n✅ All locale files have consistent keys.');
  }
}

main();
```

**验证**：`npx tsx scripts/check-i18n-consistency.ts` 运行无报错。

---

## 验证清单（每阶段完成后执行）

```bash
# 1. TypeScript 编译检查
npx vue-tsc --noEmit

# 2. Vite 构建
npx vite build

# 3. 单元测试
npx vitest run

# 4. Rust 编译检查（如有工具链）
cargo check

# 5. Flutter 分析（如有 Flutter SDK）
cd acp_ui_flutter && flutter analyze

# 6. 浏览器 E2E 验证
# 启动 dev server: npx vite --port 5173
# 打开以下页面确认无控制台错误:
#   - /dashboard
#   - /agent-teams
#   - /task-graph (确认有"演示数据"横幅)
#   - /collaboration (确认有"模拟数据"横幅)
#   - /hermes (确认有连接状态指示)
#   - /status, /monitor (确认有引导文案)
#   - /settings (确认设置值刷新后保留)
```

---

## Git 提交规范

每个阶段完成后提交一次：

```bash
# 阶段一
git commit -m "fix: P0 urgent fixes - Flutter compile error, memory leak, dead code cleanup"

# 阶段二
git commit -m "fix: P1 mock data cleanup - add demo banners, connection status indicators"

# 阶段三
git commit -m "feat: P1 frontend-backend integration - orchestration/approval/sync API + store"

# 阶段四
git commit -m "feat: P2 UX improvements - non-blocking approvals, session search/export"

# 阶段五
git commit -m "refactor: P3 architecture - Rust module grouping, i18n consistency script"
```

---

## 注意事项

1. **不要引入新的 mock 数据**。如果某个页面暂时没有真实数据，显示"演示数据"横幅而非假数据。
2. **所有 `invokeOrProxy` 调用都要 try-catch**。后端命令可能因 Tauri 未启动而失败（Web 模式），需要优雅降级。
3. **不要修改已有测试的断言**。如果需要修改测试，必须有对应的功能变更理由。
4. **每个文件修改后都要运行 `npx vue-tsc --noEmit`** 确认编译通过。
5. **Rust 文件修改后运行 `cargo check`**（如果工具链可用）。
6. **Flutter 文件修改后运行 `flutter analyze`**。
