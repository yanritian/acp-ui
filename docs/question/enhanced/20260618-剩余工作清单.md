# ACP-Swarm 剩余工作清单

> **日期**: 2026-06-18
> **状态**: 基于逐文件源码验证，所有断言已确认准确
> **编译基线**: cargo check 通过（0 errors, 39 warnings）、vue-tsc 通过、vitest 294 tests 全绿

---

## 一、必须做（阻塞性，预计半天）

### C-1：Goal 类型最终统一

这是唯一影响架构一致性的问题。acp-core 的统一类型已就绪，swarm-engine 已切换完毕，剩余工作集中在 `src-tauri/src/goal.rs` 一个文件。

**做什么**：
1. 删除本地 Goal / CompletionCondition / Evaluator / GoalStatus / IterationRecord / EvaluationResult / ConditionResult / GoalGraphSummary 的类型定义（约 600 行）
2. 改为 `pub use acp_core::{GoalRuntime as Goal, ...}`（与 swarm-engine/goal.rs 对齐）
3. 删除 to_runtime() / from_runtime() 桥接方法（约 120 行）
4. GoalGraph 内部存储从本地 Goal 改为 acp_core::GoalRuntime
5. 更新 10 个 Tauri 命令的类型签名
6. 处理 GoalStatus 差异（acp-core 9 态 vs 本地 8 态，多了 MaxIterReached）

**验证方式**：
```bash
cargo check --workspace && cargo test --workspace && npx vue-tsc --noEmit && npx vitest run
```

### N-1：GoalGraph 双写处理

src-tauri/src/ 内部有两个同名 GoalGraph：
- `goal::GoalGraph` — Mutex<HashMap>，被 AppState 和 Tauri 命令使用
- `goal_graph::GoalGraph` — 三个 HashMap 带 DAG，被 topology.rs 使用（7 处引用）

**做什么**：决定保留哪个。建议保留 goal.rs 版本（Tauri 命令使用），如果 topology.rs 的拓扑执行能力暂不需要，goal_graph.rs 可随之一并清理。

### N-7：删除空目录

- `src-tauri/src/features/swarm/` — 空目录
- `src-tauri/src-tauri/src/swarm_adapters/` — 嵌套残留目录

---

## 二、应该做（代码卫生，预计 1-2 天）

### N-2：Cargo.toml 排除幽灵 Crate

6 个 workspace members 从未被主 crate 依赖：acp-transport、acp-cli、workflow-engine、hook-runtime、goal-parser、finance-engine。

**做什么**：
```toml
# src-tauri/Cargo.toml
[workspace]
default-members = ["."]
```

效果：`cargo build` 不再编译这 6 个 crate，日常编译速度提升。

### M-1：删除废弃 mock 文件

`src/lib/collaboration/mock-data-generator.ts`（627 行）已标记 @deprecated，零导入。

**做什么**：直接删除。

### N-6：修复 self_healing.rs 硬编码假数据

`find_anomaly_by_id()`（第 462-478 行）无论传入什么 ID 都返回相同的硬编码 AnomalyRecord（type 固定 HighErrorRate，target 固定 "unknown"）。

**做什么**：替换为实际查询逻辑，或至少返回有意义的错误而非静默假数据。

### N-3：清理 dead code 标注

三个模块标注了 `#![allow(dead_code)]` 但从未被调用：agent_registry.rs（~350行）、mcp_manager.rs（~200行）、session_manager.rs（~350行）。

**做什么**：移除 `#![allow(dead_code)]`，让编译器 warning 提醒开发者这些模块尚未接入。

---

## 三、可以做（质量提升，预计 2-3 天）

### M-2：确定 Reconcile 同步/异步路径

reconcile.rs 已有完整异步实现（reconcile_once_async 第 505 行，reconcile_until_done_async 第 676 行）。但同步版 reconcile_once() 仍使用 std::thread::sleep 阻塞轮询（第 147-183 行）。

**做什么**：决定 Tauri 命令走同步还是异步路径。如果走异步，将 Tauri 命令改为调用异步版本，同步版可降级为 fallback 或移除。

### M-3：Orchestrator 轮询改事件驱动

前端 Orchestrator 等待 worker 完成时使用 setInterval 轮询。后端已有完整的 EventBus + WebSocket adapter。

**做什么**：将等待逻辑从 setInterval 改为基于 WebSocket 事件的 Promise。

### N-4：减少 Dead Code Warnings

当前 39 个 warning，主要来源：swarm_types.rs / task_partitioner.rs（旧 TaskShard 系统）、queen_lease.rs（从未被调用）、worktree.rs（从未被调用）、goal_graph.rs（与 goal.rs 重复）。

**做什么**：C-1 统一后自然减少一部分。对仍计划使用的模块（queen_lease, worktree），集成到主流程后 warning 消失。

### N-5：处理孤儿 Vue 组件

6 个 .vue 文件从未被导入：ApprovalBanner.vue、SkillEditor.vue、以及 collaboration/ 下的 4 个组件。

**做什么**：如果协作功能在近期路线图中则保留，否则移至 `src/_archived/` 或添加注释。

### L-1：补前端单元测试

- `src/lib/__tests__/goal-api.test.ts` — 测试 Tauri invoke 函数的参数构建
- `src/stores/__tests__/goal.test.ts` — 测试 useGoalStore 的 actions

### L-2：端到端验证 hello-swarm.goal.yaml

启动 Tauri dev 服务器，通过 acp-cli 提交 hello-swarm.goal.yaml，验证完整链路。

### L-3（可选）：.goal 在线编辑器

在 GoalGraphView.vue 中添加文件导入和预览功能。需先将 goal-parser crate 集成到主 crate。

---

## 四、已完成（无需操作）

以下问题在之前几轮开发中已解决，经验证确认：

| 问题 | 状态 |
|------|------|
| acp-core 统一类型 | ✅ goal_protocol.rs 已有全部类型 |
| swarm-engine 切换 | ✅ goal.rs 已完全 re-export acp-core |
| lib.rs 统一类型导出 | ✅ 已 re-export（带 Unified* 别名） |
| agent-realtime.ts mock 数据 | ✅ 已清理，纯事件驱动 |
| token-optimizer.ts mock 数据 | ✅ 已清理，从后端获取真实数据 |
| adaptive-strategy.ts 硬编码 | ✅ 0.85 是 ?? fallback，非硬编码 |
| Reconcile 异步化 | ✅ reconcile_once_async / reconcile_until_done_async 已实现 |
| Event Bus WebSocket adapter | ✅ websocket_adapter.rs 已实现 |
| ACP Server start() scaffold | ✅ 已标注 deprecated，注释已说明设计意图 |
| orchestrator.ts 真实执行 | ✅ 已调用真实 swarm API |

---

## 执行建议

**如果只有一天**：只做 C-1 + N-1 + N-7（半天），剩余时间做 N-2。

**如果有三天**：做完第一层 + 第二层全部。

**如果有五天**：全部完成，包括补测试和端到端验证。
