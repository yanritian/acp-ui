[package]
name = "goal-parser"
version = "0.1.0"
edition = "2021"

[dependencies]
acp-core = { path = "../acp-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
uuid = { version = "1", features = ["v4"] }
thiserror = "1"